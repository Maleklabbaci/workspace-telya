import React from 'react';

const TelyaLogo = ({ className }: { className?: string }) => {
  return (
    <h1 className={`text-3xl font-extrabold tracking-tight text-primary ${className}`}>
        Telya
    </h1>
  );
};

export default TelyaLogo;